安装部署
1. 生成requirements.txt
pip freeze > requirements.txt

2.
git clone
checkout 到dev分支

 pip3 install -r requirements.txt --trusted-host mirrors.aliyun.com

  pip3 install  virtualenv  --trusted-host mirrors.aliyun.com --user test
   pip3 install  ssl  --trusted-host mirrors.aliyun.com --user test
